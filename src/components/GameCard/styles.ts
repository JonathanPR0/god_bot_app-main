import { styled } from "styled-components";


export const Container = styled.div`
img{
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: auto;
  border-radius: 4px;
}
img.disabled{
  filter: grayscale(1);
}

`
